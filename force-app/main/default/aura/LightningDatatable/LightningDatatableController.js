({
    fetchAcc : function(component, event, helper) {
        //sample(component, event, helper);
        helper.fetchAccHelper(component, event, helper);
    },
    
    sample : function(component, event, helper) {
        console.log('in sample method');
    }
})